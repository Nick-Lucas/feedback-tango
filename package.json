{
  "name": "feedback-thing",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf node_modules && pnpm -r exec rm -rf node_modules",
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel --stream=true dev",
    "test": "pnpm -r test",
    "lint": "eslint . --ext .ts,.tsx",
    "lint:fix": "eslint . --ext .ts,.tsx --fix",
    "format": "prettier --check .",
    "format:write": "prettier --write . --list-different",
    "typecheck": "pnpm -r typecheck",
    "db:generate": "pnpm -C libs/db db:generate",
    "db:migrate": "pnpm -C libs/db db:migrate",
    "db:studio": "pnpm -C libs/db db:studio",
    "db:wipe": "pnpm -C libs/db db:wipe",
    "cli": "pnpm -C apps/cli cli",
    "cli-install": "pnpm install -g ./apps/cli",
    "cli-uninstall": "pnpm uninstall -g @feedback-thing/cli",
    "merge-down": "pnpm lint && pnpm format && pnpm typecheck && git checkout main && git pull origin main && git merge - --no-ff"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.13.1",
  "dependencies": {
    "dotenv": "catalog:",
    "drizzle-orm": "catalog:",
    "typescript": "catalog:"
  },
  "devDependencies": {
    "@eslint/js": "catalog:",
    "@tanstack/eslint-config": "catalog:",
    "@typescript-eslint/eslint-plugin": "catalog:",
    "@typescript-eslint/parser": "catalog:",
    "eslint": "catalog:",
    "globals": "catalog:",
    "prettier": "catalog:"
  }
}
